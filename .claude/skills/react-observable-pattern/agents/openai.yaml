interface:
  display_name: "React Observable Pattern"
  short_description: "Build React features with localized observable subscriptions"
  default_prompt: "Use $react-observable-pattern to create or refactor a React feature with a stable observable provider and subscriptions placed at the lowest necessary level."
policy:
  allow_implicit_invocation: true
